"use client"

import { Resizable as ResizablePrimitive } from "react-resizable-panels"

import { cn } from "@/lib/utils"

const ResizablePanelGroup = ResizablePrimitive.PanelGroup

const ResizablePanel = ResizablePrimitive.Panel

const ResizableHandle = ResizablePrimitive.Handle

export {
  ResizablePanelGroup,
  ResizablePanel,
  ResizableHandle,
}